namespace Jellyfin.Plugin.BulgarianSubs

open MediaBrowser.Model.Plugins

type PluginConfiguration() =
  inherit BasePluginConfiguration()
// Add custom configuration properties here if needed (e.g., credentials)
// member val ExampleSetting = "DefaultValue" with get, set
